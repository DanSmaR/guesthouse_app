<article id="advanced_search">
  <h2>Pesquisa Avançada de Pousadas</h2>
  <%= form_tag guesthouses_advanced_searches_path, method: :get do %>
    <p>
      <%= label_tag :query_advanced, 'Digite o nome, bairro ou cidade' %>
      <%= text_field_tag :query_advanced, params[:query_advanced] %>
    </p>
    <%= fields_for :options do |f| %>
      <p>
        <%= f.check_box :pets, checked: params.dig(:options, :pets) %>
        <%= f.label :pets, 'Aceita Pets' %>
      </p>
      <p>
        <%= f.check_box :accessible, checked: params.dig(:options, :accessible) %>
        <%= f.label :accessible, 'Acessível para PCDs' %>
      </p>
      <p>
        <%= f.check_box :air_conditioning, checked: params.dig(:options, :air_conditioning) %>
        <%= f.label :air_conditioning, 'Ar condicionado no quarto' %>
      </p>
      <p>
        <%= f.check_box :tv, checked: params.dig(:options, :tv) %>
        <%= f.label :tv, 'TV no quarto' %>
      </p>
    <% end %>
    <p>
      <%= submit_tag "Buscar" %>
    </p>
  <% end %>
</article>