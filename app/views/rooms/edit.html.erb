<section>
  <h2>Editar Quarto</h2>

  <%= render "form", room: @room, guesthouse: @guesthouse %>

    <p>
      <%= link_to "Mostre esse Quarto", guesthouse_room_path(@guesthouse, @room) %> |
      <%= link_to "Voltar para Quartos", guesthouse_rooms_path(@guesthouse) %>
    </p>
</section>
