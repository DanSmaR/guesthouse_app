<article>
  <h2>Quartos</h2>
  <% if guesthouse.guesthouse_owner.user == current_user %>
    <%= link_to "Cadastrar Quartos", new_guesthouse_room_path(guesthouse) %> |
  <% end %>
    <div id="rooms">
      <% rooms.each do |room| %>
        <section id="room">
          <h3><%= link_to room.name, guesthouse_room_path(guesthouse, room) %></h3>
          <dl>
            <dt>Descrição:</dt>
            <dd><%= room.description %></dd>
            <dt>Tamanho:</dt>
            <dd><%= room.size %> m<sup>2</sup></dd>
            <dt>Acomodação Máxima:</dt>
            <dd><%= room.max_people %></dd>
            <dt>Valor da Diária:</dt>
            <dd><%= number_to_currency(room.daily_rate, unit: 'R$ ', separator: ',', delimiter: '.') %></dd>
            <% if guesthouse.guesthouse_owner.user == current_user %>
              <dt>Disponibilidade:</dt>
              <dd><%= room.available == true ? 'Sim' : 'Não' %></dd>
            <% end %>
          </dl>
          <p>

          </p>
        </section>
      <% end %>
    </div>
</article>